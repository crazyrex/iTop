{% if title_level is not defined %}
    {% set title_level='=====' %}
{% endif %}
{% if blacklist is not defined %}
    {% set blacklist =['link', 'see', 'abstract', 'example', 'method', 'property', 'property-read', 'property-write', 'package', 'subpackage'] %}
{% endif %}

{{title_level}} Tags {{title_level}}
^
{% for tagName,tags in node.tags if tagName not in blacklist %}
{{ tagName }} ^
{% endfor %}
|
{% for tagName,tags in node.tags if tagName not in blacklist %}
 {% for tag in tags %}{{ tag.description}}{% endfor %}   |
{% endfor %}